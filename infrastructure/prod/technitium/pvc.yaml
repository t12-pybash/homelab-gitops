apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: technitium-data
  namespace: dns
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi
  resources:
    requests:
      storage: 2Gi
