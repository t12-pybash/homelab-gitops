apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-ingest-cache
  namespace: vault-ingest
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: truenas-iscsi
  resources:
    requests:
      storage: 100Mi
