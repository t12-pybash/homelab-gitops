apiVersion: v1
kind: ConfigMap
metadata:
  name: sre-config
  namespace: vault-ingest
data:
  config.toml: |
    ollama_model = "mistral"
    embed_ollama_url = "http://10.0.0.113:11434"
    qdrant_url = "https://qdrant.home.t12.io"
    vault_path = "/vault"
